<p style="color: green"><%= notice %></p>


<div class="container">
  <div class="row">
    <div class="col-md-2"><h4>Your Loans</h4></div>
    <%= link_to "Add New loan", new_loan_path%>
  </div>
  <div class="row mt-3">
    <% @loans.each do |loan| %>
      <%= render loan %>
    <% end %>
  </div>
</div>
